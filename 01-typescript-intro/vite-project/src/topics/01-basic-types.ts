const name = "Angel"
let hpPoints: number | "FULL" = 10
const isAlive = true

hpPoints = "FULL"

console.log({"name": name, "hpPoints": hpPoints, "isAlive": isAlive})

export {name, hpPoints, isAlive}
